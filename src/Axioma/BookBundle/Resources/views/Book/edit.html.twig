{% extends '::base.html.twig' %}

{% block body -%}
  <div class="row">
    <div class="col-md-6">
      <h1>{{ 'h.edit'|trans }}</h1>
      {{ include('AxiomaBookBundle:Book:form.html.twig', {
        'form': form,
        'tags': tags
      }) }}
    </div>
  </div>
{% endblock %}
